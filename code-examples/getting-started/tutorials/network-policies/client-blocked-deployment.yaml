apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-blocked
spec:
  selector:
    matchLabels:
      app: client-blocked
  template:
    metadata:
      labels:
        app: client-blocked
    spec:
      containers:
        - name: client
          image: alpine/curl
          command: [ "/bin/sh", "-c", "--" ]
          args: [ "while true; do curl -si server; sleep 2; done" ]