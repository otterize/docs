import React from "react"
import CodeBlock from "@theme/CodeBlock"

export default function CLI() {
    return (
        <>
            <p>
                otterize observer enable:
            </p>
            <CodeBlock language="shell">{`otterize observer enable`}</CodeBlock>
        </>
    )
}